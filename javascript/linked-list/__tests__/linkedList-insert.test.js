'use strict';

// Require our linked list implementation
const LinkedList = require('../index');

describe('Append End of Node', () => {
  it('Can successfully add a node to the end of the linked list', () => {
    expect(true).toBeTruthy();
  });
});

describe('Append multiple nodes to the end', () => {
  it('Can successfully add multiple nodes to the end of a linked list', () => {
    expect(true).toBeTruthy();
  });
});

describe('Append before a node', () => {
  it('Can successfully insert a node before a node located i the middle of a linked list', () => {
    expect(true).toBeTruthy();
  });
});

describe('append to the beginning of a linked list', () => {
  it('  Can successfully insert a node before the first node of a linked list', () => {
    expect(true).toBeTruthy();
  });
});

describe('Append after a node', () => {
  it(' Can successfully insert after a node in the middle of the linked list', () => {
    expect(true).toBeTruthy();
  });
});
describe('can append a node to the end of the linked list', () => {
  it('Can successfully insert a node after the last node of the linked list', () => {
    expect(true).toBeTruthy();
  });
});


// Can successfully add a node to the end of the linked list
// Can successfully add multiple nodes to the end of a linked list
// Can successfully insert a node before a node located i the middle of a linked list
// Can successfully insert a node before the first node of a linked list
// Can successfully insert after a node in the middle of the linked list
// Can successfully insert a node after the last node of the linked list
