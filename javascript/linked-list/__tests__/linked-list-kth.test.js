describe('k > linked list array', () => {
  it(' Where k is greater than the length of the linked list', () => {
    expect(true).toBeTruthy();
  });
});

describe('k = linked list array', () => {
  it('Where k and the length of the list are the same', () => {
    expect(true).toBeTruthy();
  });
});

describe('k is equals a negative number', () => {
  it(' Where k is not a positive integer', () => {
    expect(true).toBeTruthy();
  });
});

describe('linked list array is equals to 1', () => {
  it(' Where the linked list is of a size 1', () => {
    expect(true).toBeTruthy();
  });
});

describe(' “Happy Path” where k is not at the end, but somewhere in the middle of the linked list', () => {
  it(' Will return a linked list where k is in the middle', () => {
    expect(true).toBeTruthy();
  });
});






























// Where k is greater than the length of the linked list
// Where k and the length of the list are the same
// Where k is not a positive integer
// Where the linked list is of a size 1
// “Happy Path” where k is not at the end, but somewhere in the middle of the linked list
